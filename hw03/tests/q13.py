test = {
  'name': 'Question13',
  'points': 1,
  'suites': [
    {
      'cases': [
        {
          'code': r"""
          >>> import numpy as np
          >>> isinstance(avg_total_income, int) or isinstance(avg_total_income, float) or isinstance(avg_total_income, np.int64) or isinstance(avg_total_income, np.float64)
          True
          """,
          'hidden': False,
          'locked': False
        },
      ],
      'scored': True,
      'setup': '',
      'teardown': '',
      'type': 'doctest'
    }
  ]
}